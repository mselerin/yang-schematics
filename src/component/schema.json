{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsYangComponent",
  "title": "Yang Component Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the component.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the component.",
      "visible": false
    },
    "feature": {
      "type": "string",
      "description": "The name of the feature to create the component."
    },
    "shared": {
      "type": "boolean",
      "default": false,
      "description": "Create the component inside the shared module."
    },
    "flat": {
      "type": "boolean",
      "default": false,
      "description": "Flag to indicate if a dir is created."
    },
    "spec": {
      "type": "boolean",
      "default": true,
      "description": "Specifies if a spec file is generated."
    },
    "styles": {
      "type": "boolean",
      "default": false,
      "description": "Specifies if a style file is generated."
    },
    "template": {
      "type": "boolean",
      "default": false,
      "description": "Specifies if a template file is generated."
    },
    "routing": {
      "type": "boolean",
      "default": false,
      "description": "Specifies if a route is generated."
    }
  },
  "required": []
}
